import { ToolMessage as ToolMessageType } from "@langchain/core/messages/tool";

export function ToolMessage(_props: { message: ToolMessageType }) {
  // return (
  //   <Message message={message} prefix="󰍡" marginLeft={0} color="whiteBright" />
  // );
  return null;
}
